Train:
  batch_size: 128
  learning_rate: 1e-3
  reg: 0.0005
  epochs: 10
  steps: [6, 8]
  warmup: 0
  momentum: 0.9
  gamma: 1

data:
  image_size: 256 # Downscale to 128 for computing constraint
  channels: 3
  category: bottle

network:
  model: 

loss:
  loss_type: MSE